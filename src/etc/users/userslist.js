let _rootEnabled = true;
export const rootEnabled = _rootEnabled; // TODO: Transform it into object
export let users = [];
// Users template: [[(String: userName), (Bool: enabled), (String: password | Return: undefined if not)]]